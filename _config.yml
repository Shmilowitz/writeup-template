# ════════════════════════════════════════
#  WRITEUP TEMPLATE — _config.yml
#  Edit this file for each new writeup.
#  Everything else updates automatically.
# ════════════════════════════════════════

# ── Site identity ────────────────────────
title:       "YOUR WRITEUP TITLE"
description: "Short description of this writeup / assessment"
author:      "David Shmilowitz"

# ── GitHub Pages URL config ─────────────
# baseurl = "/YOUR-REPO-NAME"
# url     = "https://YOUR-USERNAME.github.io"
baseurl: "/your-repo-name"
url:     "https://your-username.github.io"

# ── Hero section ────────────────────────
hero_eyebrow:  "CTF // Event Name"
hero_title:    "YOUR"
hero_subtitle: "WRITEUP TITLE"

# ── Metadata badges ─────────────────────
badge_target: "target-hostname"
badge_steps:  "0"
badge_status: "In Progress"

# ── Navigation links ────────────────────
# Each item: { label, anchor }
nav_links:
  - label: "Overview"
    anchor: "#overview"
  - label: "Findings"
    anchor: "#findings"
  - label: "Exploit"
    anchor: "#exploit"
  - label: "Conclusion"
    anchor: "#conclusion"

# ── Sidebar TOC ─────────────────────────
# Each item: { label, anchor, step (optional) }
toc_links:
  - label: "Overview"
    anchor: "#overview"
  - label: "Findings"
    anchor: "#findings"
  - label: "Step One"
    anchor: "#step-one"
    step: "01"
  - label: "Step Two"
    anchor: "#step-two"
    step: "02"
  - label: "Conclusion"
    anchor: "#conclusion"

# ── External links ───────────────────────
portfolio_url: "https://your-username.github.io/portfolio"
github_repo:   "your-username/your-repo-name"

# ── Jekyll settings (do not change) ─────
markdown:  kramdown
highlighter: rouge
plugins:
  - jekyll-seo-tag
