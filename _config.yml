# ════════════════════════════════════════
#  WRITEUP TEMPLATE — _config.yml
#  Edit this file for each new writeup.
#  Everything else updates automatically.
# ════════════════════════════════════════

# ── Site identity ────────────────────────
title:       "OPERATION RINGALING-DDING"
description: "Security Assessment Report — DDING-3000 SmartBell"
author:      "David Shmilowitz"

# ── GitHub Pages URL config ─────────────
# baseurl = "/YOUR-REPO-NAME"
# url     = "https://YOUR-USERNAME.github.io"
baseurl: "/writeup-template"
url:     "https://shmilowitz.github.io"

# ── Hero section ────────────────────────
hero_eyebrow:  "CTF // FE Hackerakademiet"
hero_title:    "OPERATION"
hero_subtitle: "RINGALING-DDING"

# ── Metadata badges ─────────────────────
badge_target: "DDING-3000 SmartBell"
badge_steps:  "4"
badge_status: "Complete"

# ── Navigation links ────────────────────
# Each item: { label, anchor }
nav_links:
  - label: "Overview"
    anchor: "#overview"
  - label: "Findings"
    anchor: "#findings"
  - label: "Exploit"
    anchor: "#exploit"
  - label: "Conclusion"
    anchor: "#conclusion"

# ── Sidebar TOC ─────────────────────────
# Each item: { label, anchor, step (optional) }
toc_links:
  - label: "Overview"
    anchor: "#overview"
  - label: "Findings"
    anchor: "#findings"
  - label: "U5FS Extraction"
    anchor: "#u5fs-extraction"
    step: "01"
  - label: "update2d"
    anchor: "#update2d"
    step: "02"
  - label: "netupsrv"
    anchor: "#netupsrv"
    step: "03"
  - label: "Conclusion"
    anchor: "#conclusion"

# ── External links ───────────────────────
portfolio_url: "https://shmilowitz.github.io/portfolio"
github_repo:   "Shmilowitz/writeup-template"

# ── Jekyll settings (do not change) ─────
markdown:  kramdown
highlighter: rouge
plugins:
  - jekyll-seo-tag
